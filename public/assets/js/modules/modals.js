/**
 * Modals Module
 * Utiliza la API de Bootstrap para los modales
 */

export function initModals() {
  // Bootstrap JS se encarga automáticamente de los modales
  // Esto solo existe para mantener la coherencia con otros módulos
  console.log('Bootstrap modals initialized');
}
